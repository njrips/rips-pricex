/** Soft no-op Self-QA stub for RipsPriceX MVP (RipX Self-QA is optional). */
async function ensureDefaultSchedule() {
  return null;
}

async function startQaRun() {
  return null;
}

module.exports = {
  ensureDefaultSchedule,
  startQaRun,
};
