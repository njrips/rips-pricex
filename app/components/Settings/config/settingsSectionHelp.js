/** Long-form help moved to tooltips on section titles */
export const SECTION_HELP = {
  themePriceSelectors:
    'Shop-wide selectors for where RipsPriceX paints test prices. Used by Classic Smart Pricing when a visitor is bucketed. Prefer Suggest from theme / Auto-map, then verify with visual pick.',
};
