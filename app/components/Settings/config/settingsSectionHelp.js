/** Long-form help moved to tooltips on section titles */
export const SECTION_HELP = {
  themePriceSelectors:
    'Shop-wide selectors for where RipsPriceX paints test prices. Auto-map reads allowlisted theme files and verifies them on the live storefront. AI only ranks verified selectors — it never invents CSS. Visual pick remains the fallback.',
};
